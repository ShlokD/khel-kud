### Khel Kud

A web application for an amusement park.

The Architecture of the application is as follows:

- The API directory hosts the server endpoints
- The pages/domain directory hosts the domain model on which the UI is based
- The pages/*.js files host each individual UI page.